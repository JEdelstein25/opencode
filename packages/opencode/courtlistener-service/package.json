{
  "name": "courtlistener-service",
  "version": "1.0.0",
  "type": "module",
  "description": "CourtListener search service for legal opinions",
  "scripts": {
    "dev": "bun run server.ts",
    "start": "bun run server.ts",
    "build-index": "bun run build-index.ts",
    "import-clickhouse": "bun run clickhouse/import-to-clickhouse.ts",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "dependencies": {
    "@clickhouse/client": "^1.12.1",
    "picomatch": "^4.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/picomatch": "^4.0.2"
  }
}
